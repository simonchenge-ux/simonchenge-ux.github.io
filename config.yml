# Add to your existing _config.yml
defaults:
  - scope:
      path: ""
    values:
      layout: "default"
  - scope:
      path: "assets/css"
    values:
      layout: null

# Include custom CSS
sass:
  sass_dir: assets/css
